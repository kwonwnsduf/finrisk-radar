apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: finrisk
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/part-of: finrisk-radar
spec:
  selector:
    app.kubernetes.io/name: backend
  ports:
    - name: http
      port: 8080
      targetPort: http
